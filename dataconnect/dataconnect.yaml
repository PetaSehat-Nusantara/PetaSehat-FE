specVersion: "v1"
serviceId: "peta-sehat-fe"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "peta-sehat-fe-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
